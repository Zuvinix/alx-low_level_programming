#include <stdio.h>

/**
 * main - Describes start of program
 *
 * Return: Prints zero if there is no error otherwise print non-zero value
 */

int main(void)

{
	printf("with proper grammar, but the outcome is a piece of art,\n");
		return (0);
}
