#include <stdio.h>

/**
 * main - Serves an entry point
 *
 * Return: By default returns zero if no error itherwise return non-zero value
 */

int main(void)
{
	puts("\"Programming is like building a multilingual puzzle");
	return (0);
}
